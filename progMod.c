#include "progMod.h"

void functionProgMod(City *povos, MatAdj *cidadePovosDist, int maxPeso, int max_Dist){
    int *melhorCaminho;
    for(int i=0; i<cidadePovosDist->rows_columns;i++){ //para que eu teste começando de todas as cidades (rows_columns equivale ao numero de cidades)

    }
}